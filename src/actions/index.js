

export const userSelected = (name) => ({
    type: "USER_SELECTED",
    name
})

export const backToMain = (name) => ({
    type: "BACK_TO_MAIN",
})

export const nextScreen = (name) => ({
    type: "NEXT_SCREEN",
})
